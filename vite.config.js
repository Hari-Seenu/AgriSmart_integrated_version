import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: '/Smart-agri/' // Base path configured for GitHub Pages hosting (repository name-scoped)
})
